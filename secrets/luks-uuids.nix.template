# secrets/luks-uuids.nix.template  -  SAFE TO COMMIT (no real values)
# Copy to secrets/luks-uuids.nix and fill in your real UUID (from: sudo blkid)
{
  # LUKS container holding the shared btrfs partition, mapped as `shared`.
  sharedUuid = "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx";
}
