# secrets/personal.nix.template  -  SAFE TO COMMIT (no real values)
# Copy to secrets/personal.nix and fill in your real values.
{
  gitEmail         = "you@example.com";
  gitName          = "YourGitHubUsername";
  gitSigningPubKey = "ssh-ed25519 AAAA...key... you@example.com (git-commit-signing)";
}
